# h5 界面开发

- 标注好了的设计稿
- 交互的是html & css & js
- 先写html 结构 再写css 样式 最后写js 交互
- 文档流，盒模型 box-sizing layout布局（两列）
- 适配 
- 移动端少用，不用px 绝对大小
  用相对单位  em rem 等比例

- rem
  相对单位 相对于根元素html 字体大小 1rem = 16px

- 750 px 设计稿
  10rem(方便) = 750px html-font-size = 75px
